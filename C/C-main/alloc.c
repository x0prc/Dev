#include <stdio.h>

int main()
{

  int i, N;
  float *height;

  float sum = 0, avg;

  printf("Input the number of students \n");
  
  scanf("%d", &N);

  height = (float *) malloc(N * sizeof(float));

}
