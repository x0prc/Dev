#include <stdio.h>

int main(){

// multiple assignments
	int a,b,c;
	a = b = c = 5;
	
	char flag1 = char flag2 = 'y';


// arithmetic : +, -, /, *, %
// relational : <, >, <=, >=, ==, !=
// logical : &&, ||

	temp > 50 && a * b + c >= 25;

}